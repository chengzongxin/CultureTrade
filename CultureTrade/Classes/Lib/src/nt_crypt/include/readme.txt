nt_protocol.h 里包含的是应用协议头
nt_struct.h 里包含的是一些应用的二进制协议
nt_crypt.h 里包含的是加密的协议头和一个计算协议头里的wCrc字段校验码的值,加密算法先用 CA_ASTEA, 详细的算法实现后面再提供
nt_bu_typedef.h 里包含的是应用的协议号(协议头里的wCmd字段),比如登录号,心跳号,请求和返回用的是相同的消息号

注意所有的字段都是网络字节序(大端序)